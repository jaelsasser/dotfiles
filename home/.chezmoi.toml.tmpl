# Generated by `chezmoi init`: prompts once for the git email into [data], which
# overrides the .chezmoidata default. "Once" reads it back, so re-init won't re-prompt.
[data]
    email = {{ promptStringOnce . "email" "Git email" "103758+jaelsasser@users.noreply.github.com" | quote }}

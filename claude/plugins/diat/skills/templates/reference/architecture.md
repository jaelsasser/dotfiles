# ARCHITECTURE & friends

Evergreen description of the system's current shape and why (friends: `DESIGN.md`, system overviews). An RFC proposes a change; ARCHITECTURE describes the result — update it as the system changes.

```markdown
# Architecture

## Overview
<The system in one diagram and a paragraph.>

## Components
<Each part, its responsibility, its neighbours — a table.>

## Why it's shaped this way
<The load-bearing decisions and the constraints behind them.>

## Flow
<How a request / record / event moves through.>
```

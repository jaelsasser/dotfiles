Mirror the next batch of `[ ] task` entries into `TaskCreate`.

Unless you can one-shot exemplar writes with minimal reads (data models, golden tests, foundational abstractions), invoke the `cac:condense` Skill if its triggers are met, even if you've been told "no clarifying questions".

Crossing seam 1 is already handled by `seamster.sh`. Skip the todo list — the plan is in the window's seed. Prompt me to run `/baste` so a fresh window picks up the next seam cleanly.

Skip the baste prompt only if this is a single trivial edit (typo, log line, rename) where `/clear` ceremony costs more than it saves.

`/baste` is a mechanical renderer; fill from rich in-flow context, don't defer judgment to it.

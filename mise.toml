# Pins the repo's dev loop tooling (root, NOT deployed — beside Taskfile.yml).
# `mise install` fetches these; the Taskfile routes chezmoi through `mise exec` when
# mise is present so the pin binds. Bump deliberately, not on a moving "latest".
[tools]
chezmoi = "2.70.4"
task = "3.51.1"
